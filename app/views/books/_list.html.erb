  <div class="container">
  <% if book.errors.any? %>
    <%= book.errors.count %>errors prohibited this obj from being saved:
    <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>

      <div class="row"><h1>User info</h1></div>
        <th style="width: 40%"><%= image_tag user.get_profile_image(100,100) %></th>
        <table class="table">
            <tr>
              <th style="width: 30%">name</th>
              <td><%= user.name %></td>
            </tr>
            <tr>
              <th style="width: 30%">introduction</th>
              <td><%= user.introduction %></td>
            </tr>
          <tbody>
        </table>
        <br>
        <p class="text-center">
          <%= link_to edit_user_path(user) do %>
            <button type="button" class="btn btn-outline-secondary w-100"><i class="fas fa-user-cog"></i></button>
          <% end %>
        </p>
      <div class="row"><h1>New book</h1></div>
      <div class="row">
      <%= form_with model: book do |f| %>
        <p>Title</p>
          <%= f.text_field :title %>
        <p>Opinion</p>
          <%= f.text_area :body %>
        <br>
        <%= f.submit "Create Book", class: 'btn btn-success' %>
      <% end %>
      </div>
  </div>